{% extends "base.peb" %}

{% block title %}Edit Booking - EAJO Air{% endblock %}

{% block content %}

<div class="edit-booking-page">

<div class="auth-container">
<div class="auth-card">

    <h1>Booking #{{ booking.bookingId }}</h1>

    <div class="booking-meta">
        <p><strong>Booking Date:</strong> {{ booking.bookingDate }}</p>
        <p><strong>Status:</strong> {{ booking.status }}</p>
        <p><strong>Contact Email:</strong> {{ booking.email }}</p>
        <p><strong>Total Price:</strong> £{{ booking.totalPrice }}</p>
    </div>

    <hr>

    <h2>Flights</h2>

    {% for flight in booking.flights %}
    <div class="flight-card">
        <p><strong>{{ flight.flightNumber }}</strong></p>
        <p>{{ flight.departureCity }} → {{ flight.arrivalCity }}</p>
        <p>{{ flight.departureDate }} | {{ flight.departureTime }} → {{ flight.arrivalTime }}</p>
        <p>{{ flight.duration }}</p>
        <p>Terminal {{ flight.departureTerminal }} → Terminal {{ flight.arrivalTerminal }}</p>
    </div>
    {% endfor %}

    <hr>

    <h2>Passengers</h2>

    <form method="post" action="/edit-booking">

        {% for passenger in booking.passengers %}
        <div class="passenger-card">

            <label for="name_{{ loop.index }}">Full Name</label>
            <input type="text" id="name_{{ loop.index }}" name="name_{{ loop.index }}" value="{{ passenger.name }}">

            <label for="id_{{ loop.index }}">ID Number</label>
            <input type="text" id="id_{{ loop.index }}" name="id_{{ loop.index }}" value="{{ passenger.id }}">

            <p><strong>Type:</strong> {{ passenger.type }}</p>
            <p><strong>Seat:</strong> {{ passenger.seat }}</p>

        </div>
        {% endfor %}

        <div class="edit-actions">
            <button type="submit" class="primary-btn">Save Changes</button>
        </div>

        </form>

        <form method="post" action="/cancel-booking">
            <input type="hidden" name="bookingId" value="{{ booking.bookingId }}">
            <button
             type="submit"
              class="danger-btn"
              onclick="return confirm('Are you sure you want to cancel this booking?')"
              >
              Cancel Booking
              </button>
        </form>    

    <a href="/profile" class="back-link">← Back to Profile</a>

</div>
</div>

</div>

{% endblock %}